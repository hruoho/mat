<<echo = FALSE>>=
knit_hooks$set(inline = function(x) {
  prettyNum(x, big.mark="~", decimal="{,}")
})
p <- function(num){ 
  return(
    format(round(num*100)/100, scientific = FALSE, decimal="{,}")
)}
@
